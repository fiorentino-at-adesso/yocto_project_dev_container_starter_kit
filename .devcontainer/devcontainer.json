{
	"name": "Yocto Project Starter Kit",
	"build": {
		"dockerfile": "Dockerfile",
		"args": {
			// "USER_ID": "1000",
			// "GROUP_ID": "1000"
		}
	},
	"runArgs": [
		"--cap-add=NET_ADMIN"
	],
	"mounts": [
		"source=/home/adesso/.ssh,target=/home/builder/.ssh,type=bind,consistency=cached",
		"source=/home/adesso/devel,target=/home/builder/devel,type=bind,consistency=cached",
		"source=/dev/net/tun,target=/dev/net/tun,type=bind,consistency=cached"
	]
}